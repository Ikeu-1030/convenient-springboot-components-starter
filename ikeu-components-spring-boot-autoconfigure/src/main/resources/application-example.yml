# =============================================================================
# ikeu-components example configuration
# Copy relevant sections to your application.yml and customize as needed.
# =============================================================================

ikeu:

  # ---- Jackson JSON Configuration ----
  jackson:
    # Default date format pattern (default: yyyy-MM-dd HH:mm:ss)
    date-pattern: "yyyy-MM-dd HH:mm:ss"
    # Serialize Long/long as String to prevent JS precision loss (default: true)
    long-as-string: true
    # Serialization inclusion: non_null, non_default, non_absent, non_empty, always
    serialization-inclusion: non_null
    # Timezone for date formatting (default: system default)
    time-zone: Asia/Shanghai

  # ---- HTTP Client Configuration ----
  http-client:
    # Connection timeout (default: 10s)
    connect-timeout: 10s
    # Per-request timeout (default: 30s)
    request-timeout: 30s
    # Download timeout (default: 5m)
    download-timeout: 5m
    # Proxy settings (optional, both host and port must be set)
    # proxy-host: 127.0.0.1
    # proxy-port: 8888

  # ===========================================================================
  # ---- JWT Configuration ----
  # Supports two modes: SINGLE (one token) and DUAL (access + refresh).
  # ===========================================================================
  jwt:
    # Set to true to enable JWT auto-configuration
    enabled: false

    # Token mode: single (default) or dual
    mode: single

    # ─── Shared defaults (used as fallback) ───
    # HMAC-SHA256 secret key (at least 32 characters recommended)
    secret: "your-256-bit-secret-key-change-me-32chars"
    # Default token expiration duration
    expiration: 24h
    # Algorithm: HS256 or RS256
    algorithm: HS256
    # Default HTTP header name for token extraction
    header-name: "Authorization"
    # Default token prefix
    token-prefix: "Bearer "
    # RSA keys (only needed for RS256)
    # public-key: |
    #   -----BEGIN PUBLIC KEY-----
    #   ...
    #   -----END PUBLIC KEY-----
    # private-key: |
    #   -----BEGIN PRIVATE KEY-----
    #   ...
    #   -----END PRIVATE KEY-----

    # ─── Access token overrides (inherits from shared defaults when unset) ───
    # access-secret: "access-token-secret-min-32-chars-xxxx"
    # access-expiration: 2h
    # access-header-name: "Authorization"
    # access-token-prefix: "Bearer "

    # ─── Refresh token overrides (only used in DUAL mode) ───
    # refresh-secret: "refresh-token-secret-min-32-chars-xxx"
    # refresh-expiration: 7d
    # refresh-header-name: "X-Refresh-Token"
    # refresh-token-prefix: "Bearer "

    # ─── Filter / interceptor settings ───
    # Auto-register JwtAuthenticationFilter
    auto-filter: true
    # Ant-style path patterns excluded from JWT validation
    exclude-paths:
      - /public/**
      - /actuator/health
      - /api/v1/auth/login
      - /api/v1/auth/refresh
    # Return 401 when token is missing/invalid (default: false, delegates to app)
    fail-on-invalid: false

  # ---- OSS Configuration ----
#  oss:
#    # Set to true to enable OSS auto-configuration
#    enabled: false
#    # OSS type: aliyun or minio
#    type: aliyun
#    # OSS endpoint
#    endpoint: "https://oss-cn-hangzhou.aliyuncs.com"
#    # Credentials
#    access-key: "your-access-key"
#    secret-key: "your-secret-key"
#    # Default bucket
#    bucket: "your-bucket"
#    # CDN domain (optional, used for getPublicUrl)
#    cdn-domain: "cdn.example.com"

  # ---- Trace ID Configuration ----
#  trace:
#    # Enable request trace ID (default: true)
#    enabled: true
#    # HTTP header name for trace ID
#    header-name: X-Trace-Id
#    # Echo trace ID in response header
#    response-header: true
#    # MDC key for logging (use %X{traceId} in log pattern)
#    mdc-key: traceId

  # ---- CORS Configuration ----
#  cors:
#    # Enable CORS auto-configuration (default: false)
#    enabled: false
#    # URL pattern for CORS
#    path-pattern: /**
#    # Allowed origins
#    allowed-origins: ["*"]
#    # Allowed HTTP methods
#    allowed-methods: [GET, POST, PUT, DELETE, OPTIONS, PATCH]
#    # Allowed request headers
#    allowed-headers: ["*"]
#    # Headers exposed to the browser
#    exposed-headers: []
#    # Allow credentials (cookies, auth headers)
#    allow-credentials: false
#    # Preflight cache duration
#    max-age: 1800s

  # ---- XSS Protection ----
#  xss:
#    # Enable XSS protection (default: false)
#    enabled: false
#    # XSS mode: escape (HTML-encode) or strip (remove tags)
#    mode: escape
#    # Paths to exclude from XSS filtering
#    exclude-paths:
#      - /api/v1/richtext/**

  # ---- MyBatis-Plus Configuration ----
#  mybatis-plus:
#    # Enable MyBatis-Plus auto-configuration (default: false)
#    enabled: false
#    # Database type for pagination
#    db-type: mysql
#    # Fields auto-filled with current time on insert
#    auto-fill-create-fields: [createTime, createdAt, gmtCreate]
#    # Fields auto-filled with current time on update
#    auto-fill-update-fields: [updateTime, updatedAt, gmtUpdate]
#    # Fields auto-filled with current user ID on insert
#    auto-fill-creator-fields: [creator, createBy]
#    # Fields auto-filled with current user ID on update
#    auto-fill-updater-fields: [updater, updateBy]

  # ---- Redis Configuration ----
#  redis:
#    # Enable Redis auto-configuration (default: false)
#    enabled: false
#    # Key prefix for distributed locks
#    lock-prefix: "ikeu:lock:"
#    # Use JSON serialization for RedisTemplate values
#    use-json-serialization: true
#
#    # ─── Spring Cache (RedisCacheManager) ───
#    # Default TTL for cache entries (default: 30m)
#    cache-default-ttl: 30m
#    # TTL for null value entries to reduce cache penetration (default: 5m)
#    cache-null-ttl: 5m
#    # Prefix applied to all cache keys (default: "ikeu:cache:")
#    cache-key-prefix: "ikeu:cache:"
#    # Whether to add key prefix (default: true)
#    cache-use-key-prefix: true
#    # Whether to cache null values to prevent cache penetration (default: true)
#    cache-cache-null-values: true

  # ---- SMS Configuration ----
#  sms:
#    # Enable SMS auto-configuration (default: false)
#    enabled: false
#    # SMS provider: aliyun or tencent
#    type: aliyun
#    # Default sign name (fallback for provider-specific configs)
#    default-sign-name: "XX平台"
#    # ─── Alibaba Cloud SMS ───
#    aliyun:
#      access-key-id: "your-access-key-id"
#      access-key-secret: "your-access-key-secret"
#      sign-name: "XX平台"           # optional, overrides default-sign-name
#      region-id: cn-hangzhou
#      timeout-seconds: 10
#    # ─── Tencent Cloud SMS ───
#    tencent:
#      secret-id: "your-secret-id"
#      secret-key: "your-secret-key"
#      sdk-app-id: "1400000000"
#      sign-name: "XX平台"           # optional, overrides default-sign-name
#      region: ap-guangzhou

  # ---- Payment Gateway Configuration ----
#  payment:
#    # Enable payment auto-configuration (default: false)
#    enabled: false
#    # Payment provider: wechat or alipay
#    type: wechat
#    # ─── WeChat Pay APIv3 ───
#    wechat:
#      merchant-id: "1230000109"
#      merchant-serial-number: "xxx"
#      private-key: |
#        -----BEGIN PRIVATE KEY-----
#        ...
#        -----END PRIVATE KEY-----
#      api-v3-key: "your-api-v3-key"
#      app-id: "wx1234567890"
#      notify-url: "https://api.example.com/notify/wechat"
#    # ─── Alipay ───
#    alipay:
#      app-id: "2021000000000001"
#      private-key: "..."
#      alipay-public-key: "..."
#      gateway-url: "openapi.alipay.com"   # optional
#      notify-url: "https://api.example.com/notify/alipay"

  # ---- WebSocket / STOMP Configuration ----
#  websocket:
#    # Enable WebSocket auto-configuration (default: false)
#    enabled: false
#    # Enable JWT authentication on handshake and STOMP CONNECT (default: true)
#    jwt-auth-enabled: true
#    # Query parameter name for access token (SockJS fallback)
#    token-param: access_token
#    # STOMP endpoint paths
#    endpoint-paths: [/ws]
#    # Paths excluded from JWT validation during handshake
#    handshake-whitelist: []
#    # Allowed origins (must be explicitly configured in production)
#    allowed-origins:
#      - "https://example.com"
#    # Whether to allow SockJS fallback
#    sock-js-enabled: true

  # ---- Spring Security Protection ----
#  security-protection:
#    # Enable Spring Security protection (default: false)
#    enabled: false
#    # Disable CSRF (recommended for JWT-based APIs)
#    csrf-disabled: true
#    # Stateless session management
#    stateless-session: true
#    # Apply security headers (XSS-protection, frame-options, HSTS, etc.)
#    security-headers: true
#    # URL patterns that always permit access
#    permit-all-patterns: [/public/**, /actuator/health]
